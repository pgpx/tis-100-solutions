@0


@1
MOV UP, DOWN #X
MOV UP, RIGHT #Y
MOV UP, DOWN #W
MOV UP ACC #H
NEG
SUB 1
MOV ACC, RIGHT
MOV ACC, DOWN
MOV ACC, DOWN
#J=-H-1

@2
S:MOV LEFT, DOWN#Y
  JMP R
L:MOV -10, DOWN
  MOV -9, DOWN
  MOV -8, DOWN
  MOV -7, DOWN
  MOV -6, DOWN
  MOV -5, DOWN
  MOV -4, DOWN
  MOV -3, DOWN
  MOV -2, DOWN
  MOV -1, DOWN
  MOV 0, DOWN
  JMP S
R:JRO LEFT #J

@3


@4
S:MOV UP, ACC #X
  MOV UP, DOWN #W
  MOV UP, DOWN #J
  JMP R
L:MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  JMP S
R:JRO UP #J

@5
S:MOV UP, ACC#Y
L:MOV LEFT, DOWN#X
  MOV ACC, DOWN#Y
  SAV
  MOV UP, ACC#REM
  JEZ S
  SWP
  ADD 1
  JMP L

@6


@7


@8
S:MOV UP, ACC #W
  NEG
  SUB 2
  JMP R
L:MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  MOV ACC, RIGHT
  JMP S
R:JRO UP #J

@9
S:MOV UP, DOWN #X
  MOV UP, DOWN #Y
  JMP R
L:MOV 3, DOWN
  MOV 3, DOWN
  MOV 3, DOWN
  MOV 3, DOWN
  MOV 3, DOWN
  MOV 3, DOWN
  MOV 3, DOWN
  MOV 3, DOWN
  MOV 3, DOWN
  MOV -1, DOWN
  JMP S
R:JRO LEFT #-W-2

@10

